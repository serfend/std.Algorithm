#include <iostream>
#include <algorithm>
#include <string.h>
using namespace std;
#define MAXX 1000001
#define MAXO 2000001
char hexInfo[MAXX];
char octInfo[MAXO]
void func(){
	scanf("%s",&hexInfo);
	int carry=0;
	int len=strlen(hexInfo);
	for(int i=0;i<len;i++){
		octInfo
	} 
	
}
int main(){
	freopen("input1.txt","r",stdin);
	int n;
	scanf("%d",&n);
	while(n--){
		func();	
	}

	return 0;
}
